# 003: ゲーム UI

## Background
002 のロジックをブラウザで遊べるようにする。PC とスマホの両方で遊べること。

## Requirements
- `src/client/ui.js` / `style.css` / `index.html`：盤面描画、HUD（HP・Lv・XP・経過時間）、クリックで公開
- メモ：PC は右クリック、スマホはメモモード切替ボタン
- 戦闘・回復・レベルアップの短いメッセージ表示
- クリア／ゲームオーバー画面とリトライ
- 遊び方の簡単な説明
- 画面幅 360px でも横スクロールなしで遊べる

## Acceptance Criteria
- `build/local/index.html` で最初から最後まで遊べる
- スマホ幅で操作できる

## Out of Scope
- ランキング送信・表示（004）

## Open Questions
- なし
